data/
.env
frontend/dist/
frontend/public/chat-wasm/
frontend/public/crypto-wasm/
frontend/node_modules/
frontend/*.tsbuildinfo
frontend/package-lock.json
nginx/certs/
docker-compose.override.yml

# Optional OnlyOffice integration assets — populated by ./install-onlyoffice.sh.
# AGPL-3.0-or-later (upstream), kept out of the kutup repo because they are
# vendored binary builds, not source we maintain.
frontend/public/onlyoffice/dist/
frontend/public/onlyoffice/templates/
frontend/public/onlyoffice/LICENSES/
frontend/public/onlyoffice/SOURCE.json
frontend/public/onlyoffice/SBOM.spdx.json
frontend/public/onlyoffice/FILES.sha512
test-results/
tests/e2e/sanitized-results/

# Root package.json (Tauri orchestrator). Rust artifacts live under
# src-tauri/.gitignore.
node_modules/
package-lock.json

# Rust workspace (Go→Rust rewrite: crates/ + root Cargo workspace).
# src-tauri keeps its own target dir via src-tauri/.gitignore.
/target/
